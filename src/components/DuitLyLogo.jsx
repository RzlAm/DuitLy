// DuitLy logo for Settings page. Replace with your own logo if available.
import React from "react";
import { ReactComponent as ReactLogo } from "../../assets/react.svg";

export default function DuitLyLogo(props) {
  return <ReactLogo width={100} height={100} {...props} />;
}
